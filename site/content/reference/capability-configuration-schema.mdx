---
title: Capability Configuration Schema
description: Generated facts for supported capability configuration.
---

| Configuration point                             | Accepted value                                     | Default                                   |
| ----------------------------------------------- | -------------------------------------------------- | ----------------------------------------- |
| `completion.automatic`                          | `boolean`                                          | `true`                                    |
| `completion.documentation`                      | `selected \| automatic \| off`                     | `selected`                                |
| `completion.mappings.accept`                    | `non-empty key string \| false`                    | `<CR>`                                    |
| `completion.mappings.hide`                      | `non-empty key string \| false`                    | `<C-e>`                                   |
| `completion.mappings.next`                      | `non-empty key string \| false`                    | `<C-n>`                                   |
| `completion.mappings.next_arrow`                | `non-empty key string \| false`                    | `<Down>`                                  |
| `completion.mappings.previous`                  | `non-empty key string \| false`                    | `<C-p>`                                   |
| `completion.mappings.previous_arrow`            | `non-empty key string \| false`                    | `<Up>`                                    |
| `completion.mappings.scroll_documentation_down` | `non-empty key string \| false`                    | `<C-f>`                                   |
| `completion.mappings.scroll_documentation_up`   | `non-empty key string \| false`                    | `<C-b>`                                   |
| `completion.mappings.select_and_accept`         | `non-empty key string \| false`                    | `<C-y>`                                   |
| `completion.mappings.trigger`                   | `non-empty key string \| false`                    | `<C-Space>`                               |
| `completion.signature_help`                     | `boolean`                                          | `true`                                    |
| `completion.sources`                            | `dense array of lsp \| buffer \| path \| snippets` | `{ "lsp", "path", "snippets", "buffer" }` |
| `editor.clipboard`                              | `auto \| system \| osc52 \| disabled`              | `auto`                                    |
| `editor.indentation.style`                      | `spaces \| tabs`                                   | `spaces`                                  |
| `editor.indentation.width`                      | `integer 1..16`                                    | `2`                                       |
| `editor.line_numbers`                           | `absolute \| relative \| off`                      | `relative`                                |
| `editor.mappings.clear_search`                  | `non-empty key string \| false`                    | `<Esc>`                                   |
| `editor.mappings.delete_word`                   | `non-empty key string \| false`                    | `<A-BS>`                                  |
| `editor.mappings.focus_down`                    | `non-empty key string \| false`                    | `<C-j>`                                   |
| `editor.mappings.focus_left`                    | `non-empty key string \| false`                    | `<C-h>`                                   |
| `editor.mappings.focus_right`                   | `non-empty key string \| false`                    | `<C-l>`                                   |
| `editor.mappings.focus_up`                      | `non-empty key string \| false`                    | `<C-k>`                                   |
| `editor.mappings.message_pager`                 | `non-empty key string \| false`                    | `<leader>m`                               |
| `editor.mappings.save`                          | `non-empty key string \| false`                    | `<C-s>`                                   |
| `editor.persistent_undo`                        | `boolean`                                          | `true`                                    |
| `editor.splits.horizontal`                      | `below \| above`                                   | `below`                                   |
| `editor.splits.vertical`                        | `right \| left`                                    | `right`                                   |
| `editor.ui2`                                    | `boolean`                                          | `true`                                    |
| `editor.wrap`                                   | `boolean`                                          | `false`                                   |
| `editor.yank_highlight`                         | `boolean`                                          | `true`                                    |
| `formatting.lsp_fallback`                       | `if_no_formatter \| never`                         | `if_no_formatter`                         |
| `formatting.mappings.format`                    | `non-empty key string \| false`                    | `<leader>cf`                              |
| `formatting.on_save`                            | `boolean`                                          | `true`                                    |
| `formatting.timeout_ms`                         | `integer 1..60000`                                 | `1000`                                    |
| `language.diagnostics.severity_sort`            | `boolean`                                          | `true`                                    |
| `language.diagnostics.signs`                    | `boolean`                                          | `true`                                    |
| `language.diagnostics.underline`                | `boolean`                                          | `true`                                    |
| `language.diagnostics.update_in_insert`         | `boolean`                                          | `false`                                   |
| `language.diagnostics.virtual_text`             | `boolean`                                          | `true`                                    |
| `language.inlay_hints`                          | `boolean`                                          | `false`                                   |
| `language.mappings.code_action`                 | `non-empty key string \| false`                    | `<leader>ca`                              |
| `language.mappings.definition`                  | `non-empty key string \| false`                    | `gd`                                      |
| `language.mappings.hover`                       | `non-empty key string \| false`                    | `K`                                       |
| `language.mappings.next_diagnostic`             | `non-empty key string \| false`                    | `]d`                                      |
| `language.mappings.previous_diagnostic`         | `non-empty key string \| false`                    | `[d`                                      |
| `language.mappings.references`                  | `non-empty key string \| false`                    | `gr`                                      |
| `language.mappings.rename`                      | `non-empty key string \| false`                    | `<leader>cr`                              |
| `operation_feedback`                            | `errors \| all \| silent`                          | `errors`                                  |
| `tooling.check_on_startup`                      | `boolean`                                          | `true`                                    |

Selecting `editor` enables Neovim UI2 with its default message target (`cmd`). UI2 is experimental.
Set `editor.ui2 = false` to opt out. `<leader>m` opens the native `g<` message pager;
`editor.mappings.message_pager` can replace that key or disable the alias with `false`.

Language request mappings are buffer-local and follow attached clients' supported methods. An
existing native hover mapping (Neovim's default `K` callback or a direct `vim.lsp.buf.hover`
callback) satisfies the configured hover behavior and remains owned by Neovim or the configuration
owner. This applies to both default and explicitly configured `K`. Plait preserves that mapping on
detach; it removes only callbacks it installed. Descriptions and arbitrary wrappers do not establish
compatibility. Unrelated buffer-local mappings remain protected: application preflight reports
`effect.collision`, and later attachment reconciliation reports `language.mapping_collision`. Remove
the conflicting mapping, choose another key, or disable the action mapping with `false`.
